import { PlaceholderPage } from "@/components/placeholder-page";

export default function SocialPage() {
  return (
    <PlaceholderPage
      title="Social Media Posts"
      description="Curated social media posts about SA REITs. X/Twitter and LinkedIn lack usable free APIs, so this starts as manual curation."
      phase={4}
    />
  );
}
